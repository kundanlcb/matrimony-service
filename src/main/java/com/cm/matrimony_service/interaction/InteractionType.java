package com.cm.matrimony_service.interaction;

/**
 * Enum representing the different types of interactions between users.
 */
public enum InteractionType {
	INTEREST_SENT,
	SHORTLISTED,
	PASSED,
	MATCH_ACCEPTED,
	MATCH_DECLINED
}
