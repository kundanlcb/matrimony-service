## Build and Verification
- ALWAYS run `./gradlew build` (or equivalent build command) to verify that there are no compilation errors or test failures locally before running `git commit` or `git push`. Never commit or push code without checking the build first.
